extends layout 

block content
    form(id='form' method='post' action='/edit/' + person._id)
        label(for='name') Username: 
        input(type='text' name='name' value=person.name)
        br
        label(for='password') Password:
        input(type='text' name='password' value=person.password)
        br
        label(for='email') Email: 
        input(type='text' name='email' value=person.email)
        br
        label(for='age') Age: 
        input(type='text' name='age' value=person.age)
        br
        label(for='zeus') The name "Zeus" is related to the Greek word "dios". What is the meaning of this word?: 
        select(name='zeus')
            option(selected = person.zeus) #{person.zeus}
            option(value='king') King 
            option(value='Bright') Bright 
            option(value='Master') Master
            option(value='Weather') Weather
        br
        label(for='shiva') In which tradition is Shiva the Supreme Being who creates, protects and transforms?: 
        select(name='shiva')
            option(selected = person.shiva) #{person.shiva}
            option(value='country') Country
            option(value='war') War
            option(value='family') Family
            option(value='Weather') Shaivism
        br
        label(for='tsu') What is Tsukuyomi the god of?: 
        select(name='tsu')
            option(selected = person.tsu) #{person.tsu}
            option(value='moon') Moon 
            option(value='wind') Wind 
            option(value='Sun') Sun
            option(value='Night') Night
        br
        input(type='submit' value='Update Person')
        br
        a(href='/logout/') Log Out
